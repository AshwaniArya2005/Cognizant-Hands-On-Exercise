# Ignore all files by default
*

# Allow directories to be traversed
!*/
!**/

# Allow PDF files to be tracked
!*.pdf
!**/*.pdf

# Allow .gitkeep files to keep directories tracked
!.gitkeep
!**/.gitkeep

# Allow the .gitignore file itself to be tracked
!.gitignore
